# gaia-registry-lib

Minimal shared Python helpers for Gaia Skill Tree scripts and the Gaia CLI.

This package intentionally preserves the public APIs extracted from the former
`src/gaia_cli/frontmatter.py` and `scripts/lib/*.py` implementations.
