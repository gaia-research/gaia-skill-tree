# Sprint handover — issues #1438, #1441, #1445, #1453, #1455, #1456, #1457, #1458

**Owner:** Orchestrator (this session). **Integration branch:** `claude/gaia-orchestrator-integration-3hizne` (pre-provisioned by the CCR harness for this session; treated as the sprint's `dev/*`-equivalent integration branch — all feature PRs target it, not `main`).

## Triage (done inline, not delegated)

| Issue | Verdict | Routing |
|---|---|---|
| #1438 meta-audit: Suite lens should account for suiteComponents | **Likely already fixed.** `docs/js/skill-explorer.js` (commit `3d1b9e28e`, already on `main`) added an outboard "Suite Components" rail (`ownSuiteComponents` walk, ~L1764-1820) that lists **every** declared `suiteComponents` entry (including nested, including off-path), addressing the acceptance criteria verbatim ("must show the actual suite component names and include all declared suite components, including nested items"). Marcus flagged the same suspicion in the issue comment. | Verify last (render check), then close with evidence — no new PR expected unless verification finds a gap. |
| #1441 gaia install reports success on 34 skills | Well-scoped bug fix. Single function (`_install_single` in `src/gaia_cli/install.py`), clear proposed fix in the issue body (3 validation checks before returning `True`). | Direct dispatch, no plan doc. |
| #1445 22 skills have links.github pointing at non-skill dirs | **Not well-scoped for direct dispatch** — 22 individual judgment calls (relink vs `installable:false` vs successor-repo redirect), plus a design question about teaching `install_parity.py` about `suiteComponents` (3 `disler/*` are false positives). Issue already carries a thorough research comment with a per-skill recommendation table — use it as the input, don't re-research from scratch. | Plan-first. Save for last (before #1438 verification). Plan doc lives in `founder/handovers/`. |
| #1453 caveman — looking for true owner | Narrow: upstream deleted the skill (author confirms duplicate, never meant to be public, no replacement). Registry action is removal/freeze, not a rename. | Direct dispatch (curation action via `gaia dev`, needs `GAIA_OPERATOR_OVERRIDE=1`). |
| #1455 BUG: upstream watcher nested suite diff misclassifies | Well-scoped bug fix, bounded to `scripts/upstream_watcher/liveness.py` (`fetch_component_diff`) + a regression test fixture for `mattpocock/skills`-style nesting. | Direct dispatch. |
| #1456 CLI: gaia dev rename should update all reference surfaces | Well-scoped enhancement, bounded to `src/gaia_cli/commands/dev/rename.py`, with a clear gap list (suite manifests, skill-tree JSON, stale-reference report) from the #1452 postmortem. | Direct dispatch. |
| #1457 Upstream watcher: flag SKILL.md frontmatter name drift | Well-scoped enhancement, bounded to the upstream watcher's tracked-component sweep + a synthetic regression test. | Direct dispatch. |
| #1458 DevEx: make full pytest run hermetic in Windows/pi shell | Well-scoped DevEx fix: dependency-aware skips (`wheel`, `textual`), force non-interactive paths in push dry-run tests, standardize `PYTHONIOENCODING`. | Direct dispatch. |

## Dispatch plan

- 6 direct-implementation workers in parallel: #1441, #1453, #1455, #1456, #1457, #1458. Each opens its own feature branch off `origin/claude/gaia-orchestrator-integration-3hizne`, PR base = same, **draft PR**, 1 PR per issue.
- 1 planning-only agent for #1445 → writes `founder/handovers/2026-08-05-plan-1445-links-github.md`, no code, no registry mutation.
- #1438 verified last, after the above land — render check against the rail logic, close with a comment citing the commit if confirmed, otherwise file what's missing.

## CI posture

Per Marcus: stubborn CI red is acceptable after 2 fix attempts — leave it red rather than burn further rounds. Orchestrator decides case-by-case which PRs are safe to merge into the integration branch directly vs. leave open for founder gate (registry-mutating / redaction / evidence-affecting work defaults to gated; mechanical CLI/test/watcher fixes with green CI are candidates for direct integration-branch merge).

## Token spend

Logged per-commit on the governing issue/PR per CLAUDE.md convention; aggregate summary posted to this handover (or MEMORY) at session close.
