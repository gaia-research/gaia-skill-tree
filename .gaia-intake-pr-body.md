## Daily Curation: Token Observability

### 🛠 Installation Report
- **Status:** Success
- **Bugs Found:** None

### 🧪 Testing & Use Case
- **Experimental Findings:** Tested the `codeburn` CLI by reading session data from disk for AI coding tools. It analyzes costs and token usage, supports macOS menubar and TUI formats, and runs completely locally without a proxy. It fails elegantly if no data is found for the given period.
- **Recommended Use Case:** Tracking per-project and per-model AI API spends, identifying token waste for optimization, and analyzing the "yield" (how much of the AI spend actually made it into commits).

### 📝 Documentation Improvements
- Refined directives in `registry/skills/`.
- Synced metadata in `registry/nodes/`. Expanded the description to detail how the skill accesses local session data and added a summary field.

### ⚖️ Audit & Meta Changes
- No gaia-audit specific findings applied, as the schema passed all existing constraint checks after the update.
